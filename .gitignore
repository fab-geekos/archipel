# Outillage local Claude Code (ne pas publier sur GitHub)
.claude/

# Secrets : NE JAMAIS COMMITTER
serviceAccountKey.json
*serviceAccount*.json
.env
.env.*
jeton*
*token*.txt
*.pem
*.key

# Fichiers d'origine des visuels (les PNG lourds sortis d'ImageFX). Seule la
# version webp de images/ part en ligne. A garder ici plutot que sur le bureau :
# le projet reste autonome, et rien de lourd ne transite par GitHub.
sources/

# Dependances / OS / editeurs
node_modules/
.DS_Store
Thumbs.db
.vscode/
.idea/
